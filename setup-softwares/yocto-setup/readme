To build container:
    docker build -t yocto-build .


To run the container:
    docker run -it -v images:/home/yocto/workspace --name yocto-container yocto-build


Inside container:
    -   cd /home/yocto/poky/
    -   source oe-init-build-env
    -   bitbake-layers add-layer ../meta-dspcoder
    -   bitbake dspcoder

